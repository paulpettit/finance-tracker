/*
 * Charts for the Finance Tracker Dashboard
 *
 * TODO (Phase 4): This is where you'll add Chart.js visualizations.
 *
 * STEPS WHEN YOU'RE READY:
 * 1. Add Chart.js to your base.html:
 *    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
 *
 * 2. Add a <canvas> element in index.html where you want the chart
 *
 * 3. Fetch data from the API:
 *    fetch('/api/transactions')
 *      .then(response => response.json())
 *      .then(data => {
 *          // Process data and create chart here
 *      });
 *
 * 4. Create a chart:
 *    new Chart(document.getElementById('myChart'), {
 *        type: 'line',  // or 'pie', 'bar', 'doughnut'
 *        data: { ... },
 *        options: { ... }
 *    });
 *
 * RESOURCES:
 * - Chart.js docs: https://www.chartjs.org/docs/
 * - Good tutorial: search "Chart.js getting started tutorial"
 */

console.log('Finance Tracker loaded. Charts coming soon!');
